# ApiErrorId

An enum representing and API error.

```jsonc
"UNEXPECTED"  // string, enum: "UNEXPECTED", "UNAUTHORIZED", "FORBIDDEN", "TOKEN_EXPIRED", "NOT_FOUND", "TOO_MANY_REQUESTS", "HEADER_INVALID_ASCII_CHARACTERS", An enum representing and API error.
```
