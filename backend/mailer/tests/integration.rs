use mailer::*;

#[test]
fn builder() {
    // let _mailer = Mailer::new(server, user, password);
    // let mail = _mailer.
    // Email::new(Templates::NewMessage{reference: "test".into()})
    // .language(Language::French)
    // .build()
    // .send();
}
