# api

Endpoint declarations and the SWR services that call them. `<domain>.ts` holds paths and types with no side effects; `service/<domain>.ts` holds the hooks. See [01 – API layer](../../docs/architecture/01-api.md).
