# pages

One folder per route. A page folder is private — nothing outside it may import from its `components/`, `hooks/` or `sections/`, and no page imports another page. See [04 – Pages & router](../../docs/architecture/04-pages-router.md).
