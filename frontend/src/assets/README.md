# `assets/`

Shared static assets imported by app code (e.g. `logo.svg`). Import them through the `@/` alias or a
relative path; Vite fingerprints and bundles them.

Feature-specific assets can live in a feature's own `assets/` folder. Public, unprocessed files that
must keep a stable URL go in the top-level `public/` directory instead.
