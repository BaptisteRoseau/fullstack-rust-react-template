# components

Domain-aware components shared by more than one page. A component used by exactly one page lives in that page's `components/` folder instead. See [03 – Shared components](../../docs/architecture/03-components.md).
