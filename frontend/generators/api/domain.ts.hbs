{{! The only file in the domain that calls the generated SDK. }}
import { apiCall } from '@/api/client'
import { get{{pascalCase name}}, list{{pascalCase name}} } from '@/api/generated'

import { fromGet{{pascalCase name}}Response } from './converters'
import type { {{pascalCase name}} } from './types'

export async function fetch{{pascalCase name}}List(): Promise<{{pascalCase name}}[]> {
    const response = await apiCall(() => list{{pascalCase name}}())
    return response.map(fromGet{{pascalCase name}}Response)
}

export async function fetch{{pascalCase name}}(id: string): Promise<{{pascalCase name}}> {
    return fromGet{{pascalCase name}}Response(
        await apiCall(() => get{{pascalCase name}}({ path: { id } })),
    )
}
