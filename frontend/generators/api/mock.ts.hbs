import { vi } from 'vitest'

export const use{{pascalCase name}}List = vi.fn().mockReturnValue({
    data: [],
    error: undefined,
    isLoading: false,
    mutate: vi.fn(),
})

export const use{{pascalCase name}} = vi.fn().mockReturnValue({
    data: undefined,
    error: undefined,
    isLoading: false,
    mutate: vi.fn(),
})

export const useCreate{{pascalCase name}} = vi.fn().mockReturnValue({
    trigger: vi.fn(),
    isMutating: false,
})

export const useDelete{{pascalCase name}} = vi.fn().mockReturnValue({
    trigger: vi.fn(),
    isMutating: false,
})
