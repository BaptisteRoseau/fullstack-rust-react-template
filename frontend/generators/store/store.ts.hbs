import { create } from 'zustand'

type {{pascalCase name}}Store = {
    isEnabled: boolean
    setEnabled: (isEnabled: boolean) => void
}

export const use{{pascalCase name}} = create<{{pascalCase name}}Store>((set) => ({
    isEnabled: false,
    setEnabled: (isEnabled) => set({ isEnabled }),
}))
