[package]
name = "rbac"
version = "0.1.0"
edition = "2024"

[dependencies]
test_utils = { path = "../test_utils" }
uuid = { version = "1", features = ["fast-rng", "serde"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
uuid = { version = "1", features = ["fast-rng", "serde", "v4"] }
